# Executive Advisor

A multi-agent AI executive advisor: grounded findings, adversarial review, computed change
detection.

Scaffolding only at this commit — the modules land branch by branch. See `git log --graph`.

## Develop

```bash
pip install -e ".[dev]"
pytest -q
```

MIT licensed. See [LICENSE](LICENSE).
